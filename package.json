{
  "name": "deadware",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.mjs",
    "start": "npm run build && node server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
